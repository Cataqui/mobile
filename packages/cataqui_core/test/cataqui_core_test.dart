import 'package:flutter_test/flutter_test.dart';

void main() {
  test('cataqui_core library compiles without error', () {
    // The package successfully imports — verifying existence
    expect(true, isTrue);
  });
}
