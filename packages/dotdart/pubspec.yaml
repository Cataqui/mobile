name: dotdart
description: Convert visual assets (Lottie, SVG, etc.) to pure-Dart widget code at build time.
resolution: workspace
publish_to: none

environment:
  sdk: ">=3.12.0 <4.0.0"

dependencies:
  build: ^4.0.0
  dart_style: ^3.0.0
  glob: ^2.1.0
  meta: ^1.18.0
  path: ^1.9.0
  yaml: ^3.1.0

dev_dependencies:
  build_runner: ^2.15.0
  flutter:
    sdk: flutter
  flutter_test:
    sdk: flutter
  very_good_analysis: ^7.0.0
