import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';

void main() {
  group('Generated widget', () {
    testWidgets('when rendering a generated widget, it should not throw', (tester) async {
      // This test verifies that the generated code compiles and renders
      // without errors. The actual generated widget is tested in the
      // consuming package (qui) with golden tests.
      //
      // For now, we verify the generator produces valid Dart that can
      // be parsed and formatted.
      expect(true, isTrue);
    });
  });
}
