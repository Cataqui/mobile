name: locale
description: Shared locale catalog and type-safe translations.
publish_to: none
resolution: workspace

environment:
  sdk: ">=3.12.0 <4.0.0"

dependencies:
  intl: ^0.20.2
  slang: ^4.16.0

dev_dependencies:
  test: ^1.31.0
  very_good_analysis: ^7.0.0
