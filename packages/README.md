# Private packages

This directory is reserved for future private Mobile workspace packages.
Public packages live in their standalone Cataqui repositories and are consumed
from immutable Git tags, with ignored sibling path overrides for local work.
