# AGENTS.md - QuiHero Extensions

## Naming

All hero extensions must use the `QuiHero*Extension` naming convention.

If an extension is intended to be used only for one type of hero, include the
hero type in the name. For example, use `QuiHeroBoxDragToCloseExtension` for an
extension that only works with `QuiHero.box`.
