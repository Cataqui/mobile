part of 'qui_hero_drag_to_close_extension.dart';

/// Lifecycle state for [QuiHeroDragToCloseExtension].
enum QuiHeroDragToCloseState {
  /// No interactive close gesture is currently active.
  idle,

  /// A drag-to-close gesture is active.
  dragging,
}
