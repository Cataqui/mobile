part of 'qui_text_button.dart';

/// The side where a [QuiTextButton] icon is rendered.
enum QuiTextButtonIconPosition {
  /// Places the icon before the text.
  leading,

  /// Places the icon after the text.
  trailing,
}
