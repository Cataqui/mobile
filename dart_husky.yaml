dart_husky:
  verbose: true

pre-commit:
  parallel: false
  commands:
    analyze:
      run: melos analyze
    test:
      preset: melos
      staged_only: true

commit-msg:
  commands:
    conventional:
      preset: conventional
      types:
        append: [agent, ui]
      only_small_case: true
